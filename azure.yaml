# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: azure-blob-testing
metadata:
  template: azure-blob-testing@0.0.1-beta
services:
  blob-performance-test:
    project: ./src
    language: docker
    host: containerapp
infra:
  provider: bicep