{
  "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentParameters.json#",
  "contentVersion": "1.0.0.0",
  "parameters": {
    "environmentName": {
      "value": "${AZURE_ENV_NAME=blob-perf-test}"
    },
    "location": {
      "value": "${AZURE_LOCATION=eastus}"
    },
    "principalId": {
      "value": "${AZURE_CLIENT_ID=}"
    }
  }
}